# Server Configuration
PORT=3000
NODE_ENV=production

# Application Mode: local or cloud
# 'local' enables local folder browsing features
# 'cloud' disables filesystem-specific features for safety and compatibility
APP_MODE=local

# Database Path (Relative to server directory)
DB_PATH=./pormt.db
